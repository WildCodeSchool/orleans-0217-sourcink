{% extends "::base.html.twig" %}



{% block header %}
{% endblock %}

{% block body %}
    <h1>Tableau comparateur</h1>

    <table class="highlight">
        <thead>
        <tr>
            <th>Description</th>
            {% for product in products %}
                <th>
                    {{ product.title }}</th>
            {% endfor %}
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for benefit in benefits %}

        <tr>
            <td>{{ benefit.description }}</td>
            {% if benefit.productOptions is defined %}
                {% for productOption in benefit.productOptions %}
                    <td>{{ productOption.status }}</td>
                {% endfor %}
            {% endif %}
            <td>
                <ul>
                    <li>
                        <a href="{{ path('benefit_edit', { 'id': benefit.id }) }}"class="btn-floating btn-small waves-effect waves-light green"><i class="material-icons">mode_edit</i></a>
                    </li>
                </ul>
            </td>

            {% endfor %}


        </tr>

        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('benefit_new') }}"class="btn-floating btn-large waves-effect waves-light red"><i class="material-icons">add</i></a>
        </li>
    </ul>
{% endblock %}
